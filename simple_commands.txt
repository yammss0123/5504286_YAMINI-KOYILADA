# Copy and paste these commands into an online Linux terminal

# 1. Date commands
date
date "+%D"
date "+%D %T"
date "+%Y-%m-%d"
date "+%Y/%m/%d"
date "+%A %B %d %Y %T"
date --date "tomorrow"
date --date "2 years ago"

# 2. Calculator
bc <<< "12/5"
bc <<< "scale=2; 12/5"
bc <<< "2^10"

# 3. System info
uname
uname -o
uname -i
uname -n
uname -v
uname -s
uname -m
uname -r
uname -a

# 4. Echo commands
echo "Welcome to Linux"
echo -e "Hello \t World"
echo -n "Hello World"
echo "Welcome to \"Linux\""
echo `date`
echo "This is Linux Training for Notchup July 2025"

# 5. Printf
printf "Linux training\n"
printf "My shell is %s\n" $SHELL

# 6. Sequence
seq 1 10
seq 1 3 10
seq 10 -1 1
seq 20 -2 0
seq -s " " 10
seq -s " " 5 10
seq -s " " 5 3 50

# 7. Reverse
rev <<< "Linux"

# 8. System commands
echo $SHELL
whoami
whatis echo
whatis printenv
whatis whoami
whatis cat
whereis cat
whereis gcc

# 9. Process
ps

# 10. Directory operations
ls
mkdir new
cd new
ls
mkdir new1 new2
ls
cd
cd -
mkdir -p d1/d2
ls
cd d1
ls
ls -l
cd ..
cd
ls -l
ls -a
ls -R

# 11. File operations
touch newfile.txt
cat newfile.txt
cat newfile.txt > output.txt
cat output.txt
cat -n demo

# 12. Copy operations
mkdir training
cp newfile output.txt training
cd training
ls
cd
ls
ls -R
cp -r new training
ls
cd training
ls
cd new
ls
cd

# 13. Move operations
mv output.txt out
ls
mv out training
ls
cd training
ls

# 14. Calendar
cal
cal 7 2025
cal 5 2025
cal 2025
cal -3 2025
cal -3

# 15. Command chaining
date; uname -r; whoami
date && whoami

# 16. More file ops
touch newfile.txt
cat newfile.txt
cat newfile.txt > output.txt
cat output.txt
cat newfile.txt >> new.txt
cat new.txt
cat -n cicd
tac cicd

# 17. Remove operations
rm Day2
ls
rm n*
ls
rmdir new
rm -r new
ls

# 18. Create C program
cat > sum.c << 'EOF'
#include <stdio.h>
int main() {
    int a = 5, b = 3;
    printf("Sum: %d\n", a + b);
    return 0;
}
EOF

# 19. Compile (if gcc available)
gcc sum.c && ./a.out

# 20. History
history
history 10 